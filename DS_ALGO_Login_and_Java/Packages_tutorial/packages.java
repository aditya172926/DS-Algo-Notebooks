import static Packages_turotialmessage.messages;
// static word is used for the function messages

public class packages {
    // packages are just containers for classes, it is used to keep the class name
    // in Compartments compartmentalize
    // it is just a folder

    // we cannot make another file/package witht the same name in the same directory
    // you can make another package with the same name as this in another directory and import it here
    public static void main(String[] args) {
        System.out.println("Hello world");
        messages();
    }

}
